import React from 'react';
import Report from '/client/src/components/qComponents/answerCompontnts/aReport.jsx';
// make and add answers list to individul questions
var Answers = (props) => (

);

export default Answers;